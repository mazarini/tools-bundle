<form method="post" action="{{ path('app_son_delete', {'id': son.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ son.id) }}">
    <button class="btn">Delete</button>
</form>
