{% extends 'base.html.twig' %}

{% block title %}Son{% endblock %}

{% block body %}
    <h1>Son</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>LabelSon</th>
                <td>{{ son.labelSon }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ son.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_son_index') }}">back to list</a>

    <a href="{{ path('app_son_edit', {'id': son.id}) }}">edit</a>

    {{ include('son/_delete_form.html.twig') }}
{% endblock %}
