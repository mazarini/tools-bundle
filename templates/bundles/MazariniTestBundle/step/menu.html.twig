<div class="list-group">
	{{ dump(menu) }}
	{% embed '@MazariniTools/iterative.html.twig' with {'items': menu} %}
		{% block item_container %}
			<a href="{{ item.url }}" class="list-group-item list-group-item-action{{ item.class }}">
				{{ item.label }}
			</a>
		{% endblock item_container %}
	{% endembed %}
</div>
